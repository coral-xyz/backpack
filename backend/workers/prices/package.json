{
  "name": "prices",
  "version": "0.0.0",
  "type": "module",
  "module": "dist/index.mjs",
  "devDependencies": {
    "esbuild": "^0.14.48",
    "miniflare": "^2.5.1",
    "wrangler": "2.0.15"
  },
  "private": true,
  "scripts": {
    "start:mf": "miniflare --live-reload --debug --modules dist/index.mjs --port 9991",
    "start:local": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "node build.js"
  }
}
