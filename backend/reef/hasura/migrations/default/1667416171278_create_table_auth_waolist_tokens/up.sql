CREATE TABLE "auth"."waolist_tokens" ("username" citext NOT NULL, "minted_count" integer NOT NULL DEFAULT 0, "eligible_count" integer NOT NULL DEFAULT 1, "tx_list" jsonb NOT NULL DEFAULT jsonb_build_array(), PRIMARY KEY ("username") , FOREIGN KEY ("username") REFERENCES "auth"."users"("username") ON UPDATE cascade ON DELETE cascade, UNIQUE ("username"));COMMENT ON TABLE "auth"."waolist_tokens" IS E'Tracks waolist tokens per user';
