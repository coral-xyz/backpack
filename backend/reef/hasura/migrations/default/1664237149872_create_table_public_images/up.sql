CREATE TABLE "public"."images" ("id" serial NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "uri" text, "prompt" text NOT NULL, "invite_code" uuid NOT NULL, "completed" boolean NOT NULL DEFAULT false, PRIMARY KEY ("id") , UNIQUE ("id"), UNIQUE ("uri"), UNIQUE ("prompt"), UNIQUE ("invite_code"));
