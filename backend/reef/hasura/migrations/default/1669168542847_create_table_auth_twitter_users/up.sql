CREATE TABLE "auth"."twitter_users" ("backpack_username" citext NOT NULL, "twitter_username" citext NOT NULL, "pfp_staked_cumulative" numeric NOT NULL DEFAULT 0, PRIMARY KEY ("twitter_username","backpack_username") , FOREIGN KEY ("backpack_username") REFERENCES "auth"."users"("username") ON UPDATE cascade ON DELETE cascade, UNIQUE ("backpack_username"), UNIQUE ("twitter_username"));COMMENT ON TABLE "auth"."twitter_users" IS E'Connects backpack users to twitter users';
