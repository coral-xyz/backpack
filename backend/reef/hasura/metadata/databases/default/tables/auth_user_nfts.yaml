table:
  name: user_nfts
  schema: auth
insert_permissions:
  - role: auth_worker
    permission:
      check: {}
      columns:
        - blockchain
        - collection_id
        - nft_id
        - public_key
select_permissions:
  - role: auth_worker
    permission:
      columns:
        - blockchain
        - collection_id
        - nft_id
        - public_key
      filter: {}
      allow_aggregations: true
delete_permissions:
  - role: auth_worker
    permission:
      filter: {}
