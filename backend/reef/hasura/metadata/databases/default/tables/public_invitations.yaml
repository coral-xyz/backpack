table:
  name: invitations
  schema: public
select_permissions:
  - role: invites_worker
    permission:
      columns:
        - claimed_at
        - id
      filter: {}
  - role: mod
    permission:
      columns:
        - claimed_at
        - id
      filter: {}
